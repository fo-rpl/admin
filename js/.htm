cd /sdcard/admin

git config --global user.email "irulirul23634@gmail.com"

git config --global user.name "irul441"

git init

git *

git commit -m "ad-min"

git branch -M main

git remote add origin https://github.com/irul441/ad-min.git

git push -u origin main